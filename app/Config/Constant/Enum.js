const TYPE = {
  MESSAGE: 'message',
  STATUS: 'status',
  CONNECTION_STATUS: 'connection_status',
};

const CONNECTION_STATUS = {
  ONLINE: 'Online',
  OFFLINE: 'Offline',
}

module.exports = { TYPE, CONNECTION_STATUS };
